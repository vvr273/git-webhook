# Jira Integration Placeholder

This package is intentionally present so the project structure is ready for the next platform.

Recommended future modules here:
- `normalizers.py`
- `webhook_service.py`
- `schemas.py`
- `client.py`

The GitHub integration already uses the same pattern under:
- `app/integrations/github/`
